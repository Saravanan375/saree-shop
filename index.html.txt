<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Saree Shop</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background: #f4f4f4;
    }
    header {
      background: maroon;
      padding: 20px;
      color: white;
      text-align: center;
    }
    .container {
      width: 90%;
      margin: auto;
    }
    h2 {
      margin-top: 30px;
      text-align: center;
    }
    #productArea {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 20px;
      padding: 20px;
    }
    .product-card {
      background: white;
      padding: 15px;
      border-radius: 6px;
      box-shadow: 0 0 5px #ccc;
      text-align: center;
    }
    .product-card img {
      width: 100%;
      height: 230px;
      object-fit: cover;
      border-radius: 4px;
    }
    .product-card button {
      background: maroon;
      color: white;
      padding: 8px 16px;
      border: none;
      cursor: pointer;
      margin-top: 10px;
    }
  </style>
</head>

<body>

<header>
  <h1>Silk Saree Shop</h1>
  <p>Premium Sarees — Kanchipuram | Banarasi | Mysore</p>
</header>

<div class="container">
  <h2>Available Sarees</h2>
  <div id="productArea">Loading products...</div>
</div>

<script>
const API_URL = "https://script.google.com/macros/s/AKfycbyi-0v150TEXg5a3i0vAg6KO6NMcBvIsPOfUa-jFYgSw3u6ItEphANbBkqzU_hLwMYlUQ/exec
"; // 🔴 IMPORTANT – Replace this

fetch(API_URL + "?action=inventory")
  .then(res => res.json())
  .then(data => {
    if (!data.ok) {
      document.getElementById("productArea").innerHTML = "Failed to load inventory.";
      return;
    }
    renderProducts(data.inventory);
  })
  .catch(err => {
    document.getElementById("productArea").innerHTML = "Error fetching data";
    console.log(err);
  });

function renderProducts(list) {
  const area = document.getElementById("productArea");
  area.innerHTML = "";

  list.forEach(item => {
    area.innerHTML += `
      <div class="product-card">
        <img src="${item.image_url}" alt="${item.name}">
        <h3>${item.name}</h3>
        <p>₹${item.price}</p>
        <p>${item.description}</p>
        <button>Add to Cart</button>
      </div>
    `;
  });
}
</script>

</body>
</html>
